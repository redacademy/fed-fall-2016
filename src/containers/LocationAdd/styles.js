import { StyleSheet } from 'react-native'
import { padding } from '../../config/styles'

const styles = StyleSheet.create({
    filterContainer: {
        paddingTop: padding.top*0.5,
    },
    buttonContainer: {
        paddingTop: padding.top,
        paddingBottom: padding.bottom,
    },
})

export default styles
